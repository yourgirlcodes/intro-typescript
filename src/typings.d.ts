// declaration file is picked up automatically by typescript
// hold typechecking for stuff that isnt actually typescript eg npm modules

// declare module 'lodash'{
//     export function random(min: number, max: number): number
// }

// but actually can use DefinitelyTyped module which defines types etc for us 
// npm module
